<UserControl x:Class="se306p2.Pages.CourseButton"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 mc:Ignorable="d"
			 Name="UserControl"
			 Height="125"
			 Width="250"
             Margin="20,20,20,20">
    <Grid Grid.Row="0"
		  Grid.Column="0"
		  Background="{StaticResource CourseButton}"
		  >
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0"
				   TextAlignment="center"
				   VerticalAlignment="Center"
				   HorizontalAlignment="Stretch"
				   FontSize="20"
				   Foreground="white"><Run Text="{Binding  ElementName=UserControl, Path=CourseCode}" /></TextBlock>
        <TextBlock Grid.Row="1"
				   TextAlignment="Center"
				   VerticalAlignment="Center"
				   HorizontalAlignment="Stretch"
				   FontSize="15"
				   Foreground="LightGray"><Run Text="{Binding  ElementName=UserControl, Path=CourseTitle}" /></TextBlock>
    </Grid>
</UserControl>
